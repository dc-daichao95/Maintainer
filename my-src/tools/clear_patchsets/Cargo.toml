[package]
name = "clear_patchsets"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = { version = "1", features = ["full"] }
libsql = "0.9"
clap = { version = "4", features = ["derive"] }
anyhow = "1"
tracing = "0.1"

[dev-dependencies]
tempfile = "3"
